services:
  mqttapp:
    build: .
    container_name: mqttapp
    environment:
      - TZ=America/Argentina/Buenos_Aires
      - SERVIDOR=${SERVIDOR}
      - TOPICO1=${TOPICO1}
      - TOPICO2=${TOPICO2}
      - TOPICOSUB=${TOPICOPUB}
    restart: unless-stopped